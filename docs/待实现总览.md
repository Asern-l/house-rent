# 待实现总览（统一维护）

## 文档目的
- 汇总当前所有未完成能力与预留闭环项。
- 作为唯一待实现清单长期维护，避免多文档分裂与重复。

## 优先级说明
- P0：高必要性，建议优先实现。
- P1：中必要性，建议第二阶段实现。
- P2：增强项，可按时间选择实现。

## A. 合同签署与证据链

当前 P1 已完成，剩余为 P2 或生产级增强。

## B. 支付与链上准入

当前 P1 已完成，剩余为 P2 或生产级增强。

## C. 合同协商与生命周期

当前 P1 已完成，剩余为 P2 或生产级增强。

## D. 房源业务与治理

### 18. 房源审核流（P2）
- 现状：关键变更操作即时生效，缺少审核关口。
- 建议：高风险操作走“待审核 -> 通过/拒绝”，保留审核意见与审核人。

## E. AI 评分与图片哈希能力

### 26. 防滥用与风控（P2）
- 建议：对重复评分调用增加缓存/限频。

## F. 合规与平台能力

### 27. 押金托管与纠纷全链路（P2）
- 现状：合约定义了押金相关函数，系统侧未接入。
- 缺失：后端路由、前端页面、运营流程与证据链。

### 28. 实名/CA 体系预留接口（P2）
- 现状：仅钱包地址绑定，未引入实名/证书体系。
- 建议：预留实名认证状态字段与外部回调接口。

### 29. 评价模块（P2）
- 现状：无租后互评。
- 建议：基于已结束合同开放双向评价。

### 30. 仲裁模块（P2）
- 现状：缺少争议受理、证据提交、处理回写流程。
- 建议：建立争议工单与证据链闭环。

### 31. 聊天协商模块（P2）
- 现状：缺少站内协商渠道。
- 建议：增加签约前/履约期沟通留痕能力。

## G. 环境与开发工具链

当前 P1 已完成，剩余为 P2 或生产级增强。

## 更新规则（永久维护）
1. 新增待办：按上述分类新增，必须标优先级与现状。
2. 完成事项：从本文件删除，并迁移到 `docs/已实现总览.md`。
3. 禁止重复：不得再新建“分散待实现文档”，统一维护本文件。


## 2026-05-16 增量记录（待实现同步）
- 状态更新：房源图片上传闭环已完成基础版本与 P1 管理能力。
- 生产级增强：对象存储（OSS/S3）、缩略图/压缩处理可作为后续工程化任务。
- 关联文件：`apps/backend/src/routes/listings.js`、`apps/frontend/src/pages/PublishListing.jsx`、`apps/backend/src/index.js`。
