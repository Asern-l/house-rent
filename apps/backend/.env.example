JWT_SECRET=change_me_to_a_long_random_string
PORT=3000
AUTH_PORT=3005
# 说明：若使用 scripts/start-sepolia-services.ps1 或 scripts/start-local-services.ps1 启动，
# 不要手动改 CHAIN_ENV；脚本会注入并覆盖当前进程的 CHAIN_ENV。
CHAIN_ENV=sepolia

JSON_BODY_LIMIT=100mb
