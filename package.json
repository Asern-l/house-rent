{
  "name": "onchain-housing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix apps/backend run dev",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "build:frontend": "npm --prefix apps/frontend run build",
    "test:env-isolation": "node scripts/test_env_isolation.js",
    "check:abi": "node scripts/sync_abi_and_deployment.js --check",
    "sync:abi": "node scripts/sync_abi_and_deployment.js --write"
  }
}

